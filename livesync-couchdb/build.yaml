build_from:
  aarch64: ghcr.io/hassio-addons/base:15.0.8
  amd64: ghcr.io/hassio-addons/base:15.0.8
  armv7: ghcr.io/hassio-addons/base:15.0.8
args:
  BUILD_ARCH: "{arch}"
  BUILD_DATE: "{date}"
  BUILD_DESCRIPTION: "CouchDB server for Obsidian LiveSync"
  BUILD_NAME: "LiveSync CouchDB"
  BUILD_REF: "{ref}"
  BUILD_REPOSITORY: "{repository}"
  BUILD_VERSION: "{version}"
